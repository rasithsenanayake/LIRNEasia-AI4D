export function PrototypeNotice() {
  return (
    <aside className="border-b border-line bg-accent-wash px-4 py-2.5 text-center text-meta text-ink-soft">
      <span className="font-semibold uppercase tracking-[0.08em] text-accent-dark">Proposal prototype</span>{' '}
      Content, statistics, profiles, publications, events and datasets are illustrative unless explicitly identified as verified.
    </aside>
  );
}
